<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/purpleMain</item>
        <item name="colorPrimaryDark">@color/purpleMainDarker</item>
        <item name="colorAccent">@color/gold</item>
        <item name="android:colorBackground">@color/purpleMainDarker</item>
        <item name="android:navigationBarColor">@color/midnightBlue</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:colorForeground">@color/purpleMain</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:textColorSecondaryInverse">@android:color/white</item>
        <item name="android:statusBarColor">@color/midnightBlue</item>
        <item name="android:textColorPrimaryInverse">@android:color/white</item>
    </style>

</resources>
